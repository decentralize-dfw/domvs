# VEA Demo 4 — central config reference
# -----------------------------------------------------------------
# xlsx-loader-4.js reads the first non-empty, non-comment line below
# and treats it as the URL to the config xlsx. Relative paths resolve
# against this file's location (the demo4 folder), absolute http(s)
# URLs are used as-is. Update the active line to switch the whole
# system to a different source.
#
# Supported sources:
#   1. Local demo4 copy (production, stable — ships with the site,
#      no external dependency, always available)
#   2. Google Sheets live export (development — edits visible
#      instantly, no redeploy required)
#
# To switch sources just comment / uncomment the appropriate line.
# -----------------------------------------------------------------

# --- [active] Local production xlsx (stable) ---
demo4-input.xlsx

# --- [hidden] Google Sheets live export (development) ---
# https://docs.google.com/spreadsheets/d/1ZgfGdKYuYvWQYtS0ZVOqDe0hJ-dOHo1CJb1X39bnC0o/export?format=xlsx
